mutation Login($loginOptions: LoginOptions!) {
  login(LoginOptions: $loginOptions) {
    users {
      id
      email
      username
      createdAt
      updatedAt
    }
    errors {
      field
      message
    }
  }
}
